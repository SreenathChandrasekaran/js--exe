// Creating arrays

var scores;
var names;
var akaScores;

scores = [ 3, 1, 8, 2, 6 ];
names = [ "Kandra", "Dax", "Blinky", "Messi" ];

akaScores = scores;

console.log(scores);
console.log(names);

console.log(scores);


/* Further Adventures
 *
 * 1) Add an extra element to each array.
 *
 * 2) Declare a third variable, akaScores.
 *
 * 3) Assign the scores array to akaScores.
 *    akaScores = scores
 *
 * 4) Log akaScores to the console.
 *
 */